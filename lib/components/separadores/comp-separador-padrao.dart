import 'package:flutter/material.dart';

class SeparadorPadrao extends StatelessWidget {
  const SeparadorPadrao({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      thickness: 1,
      height: 30,
    );
  }
}
